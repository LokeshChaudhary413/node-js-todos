<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/common.css">
    <link rel="stylesheet" href="/css/dashboard.css">
    <link rel="stylesheet" href="/css/task-management.css">
    <link rel="stylesheet" href="/css/nav-fix.css">
    <link rel="icon" href="https://cdn-icons-png.flaticon.com/512/7692/7692809.png">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.3.0/css/all.min.css">
    <style>
        /* Form Container Styles */
        .forms-container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 30px;
        }

        @media (max-width: 768px) {
            .forms-container {
                grid-template-columns: 1fr;
            }
        }

        /* Form Card Styles */
        .form-card {
            background-color: var(--dark-bg);
            border-radius: 10px;
            padding: 20px;
            box-shadow: var(--card-shadow);
        }

        .form-card h2 {
            color: var(--light-text);
            font-size: 1.5rem;
            margin-top: 0;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 1px solid var(--border-color);
            display: flex;
            align-items: center;
            gap: 10px;
        }

        /* Form Styles */
        .input-field {
            margin-bottom: 15px;
        }

        .input-field label {
            display: block;
            margin-bottom: 8px;
            font-size: 0.95rem;
            font-weight: 500;
            color: var(--light-text);
        }

        .input-field input,
        .input-field select,
        .input-field textarea {
            width: 100%;
            padding: 12px 15px;
            border-radius: 8px;
            background-color: var(--darker-bg);
            border: 1px solid var(--border-color);
            color: var(--light-text);
            font-size: 0.95rem;
            transition: var(--transition);
        }

        .input-field input:focus,
        .input-field select:focus,
        .input-field textarea:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 2px rgba(35, 150, 179, 0.2);
        }

        .input-field textarea {
            resize: vertical;
            min-height: 100px;
        }

        /* Date-Time Section */
        .date-time-section {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            margin-bottom: 15px;
        }

        /* Button Styles */
        .form-actions {
            display: flex;
            justify-content: flex-end;
            gap: 10px;
            margin-top: 20px;
        }

        .btn {
            padding: 10px 16px;
            border-radius: 6px;
            font-size: 0.9rem;
            font-weight: 500;
            cursor: pointer;
            transition: var(--transition);
            border: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .btn-primary {
            background-color: var(--primary-color);
            color: white;
        }

        .btn-primary:hover {
            background-color: var(--primary-hover);
        }

        .btn-secondary {
            background-color: var(--secondary-color);
            color: white;
        }

        .btn-secondary:hover {
            background-color: var(--secondary-hover);
        }

        .btn-outline {
            background-color: transparent;
            border: 1px solid var(--border-color);
            color: var(--light-text);
        }

        .btn-outline:hover {
            background-color: rgba(255, 255, 255, 0.05);
            border-color: var(--primary-color);
        }

        /* Color Picker Styles */
        .color-picker-container {
            display: flex;
            gap: 10px;
            margin-bottom: 15px;
        }

        .color-option {
            width: 30px;
            height: 30px;
            border-radius: 50%;
            cursor: pointer;
            border: 2px solid transparent;
            transition: var(--transition);
        }

        .color-option:hover, .color-option.selected {
            transform: scale(1.1);
            border-color: white;
        }

        /* Icon Picker Styles */
        .icon-picker-container {
            display: grid;
            grid-template-columns: repeat(5, 1fr);
            gap: 10px;
            margin-bottom: 15px;
        }

        .icon-option {
            width: 40px;
            height: 40px;
            border-radius: 8px;
            cursor: pointer;
            border: 2px solid transparent;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: var(--darker-bg);
        }

        .icon-option img {
            width: 24px;
            height: 24px;
        }

        .icon-option:hover, .icon-option.selected {
            transform: scale(1.1);
            border-color: var(--primary-color);
            background-color: rgba(35, 150, 179, 0.1);
        }

        /* Flash Messages */
        .flash-messages {
            margin-bottom: 20px;
        }

        .flash-message {
            padding: 12px 15px;
            border-radius: 8px;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .flash-success {
            background-color: rgba(76, 175, 80, 0.2);
            color: #4CAF50;
            border: 1px solid rgba(76, 175, 80, 0.3);
        }

        .flash-error {
            background-color: rgba(244, 67, 54, 0.2);
            color: #F44336;
            border: 1px solid rgba(244, 67, 54, 0.3);
        }

        /* Task List Section */
        .task-list-section {
            margin-top: 30px;
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
            padding-bottom: 10px;
            border-bottom: 1px solid var(--border-color);
        }

        .section-header h2 {
            color: var(--light-text);
            font-size: 1.5rem;
            margin: 0;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        /* Category List Section */
        .category-list-section {
            margin-top: 30px;
        }

        .category-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 15px;
        }

        .category-card {
            background-color: var(--dark-bg);
            border-radius: 10px;
            padding: 15px;
            box-shadow: var(--card-shadow);
            transition: var(--transition);
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .category-card:hover {
            transform: translateY(-5px);
        }

        .category-icon {
            width: 50px;
            height: 50px;
            margin-bottom: 10px;
        }

        .category-name {
            font-size: 1rem;
            font-weight: 600;
            color: var(--light-text);
            margin-bottom: 5px;
            text-align: center;
        }

        .category-count {
            font-size: 0.9rem;
            color: rgba(207, 209, 208, 0.7);
            margin-bottom: 10px;
        }

        .category-actions {
            display: flex;
            gap: 10px;
            margin-top: auto;
        }

        .category-actions button {
            padding: 5px 10px;
            border-radius: 4px;
            font-size: 0.8rem;
            cursor: pointer;
            transition: var(--transition);
            border: none;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 5px;
        }

        .edit-btn {
            background-color: var(--warning-color);
            color: #333;
        }

        .delete-btn {
            background-color: var(--danger-color);
            color: white;
        }
    </style>
</head>
<body>
    <div class="flash-messages">
        <% if (messages.success && messages.success.length > 0) { %>
            <div class="flash-message flash-success">
                <i class="fas fa-check-circle"></i> <%= messages.success %>
            </div>
        <% } %>
        <% if (messages.error && messages.error.length > 0) { %>
            <div class="flash-message flash-error">
                <i class="fas fa-exclamation-circle"></i> <%= messages.error %>
            </div>
        <% } %>
    </div>

    <div class="forms-container">
        <!-- Task Form -->
        <div class="form-card">
            <h2><i class="fas fa-tasks"></i> Create New Task</h2>
            <form id="task-form" action="/tasks" method="POST">
                <div class="input-field">
                    <label for="task-title"><i class="fas fa-heading"></i> Task Title</label>
                    <input type="text" id="task-title" name="title" placeholder="Enter task title" required>
                </div>

                <div class="input-field">
                    <label for="task-description"><i class="fas fa-align-left"></i> Description</label>
                    <textarea id="task-description" name="description" placeholder="Enter task description"></textarea>
                </div>

                <div class="input-field">
                    <label for="task-category"><i class="fas fa-tag"></i> Category</label>
                    <select id="task-category" required>
                        <option value="">Select a category</option>
                        <% if (categories && categories.length > 0) { %>
                            <% categories.forEach(function(category) { %>
                                <option value="<%= category.name.toLowerCase() %>"
                                        data-category-id="<%= category._id %>"
                                        data-category-name="<%= category.name.toLowerCase() %>">
                                    <%= category.name %>
                                </option>
                            <% }); %>
                        <% } else { %>
                            <option value="others" data-category-name="others">Others</option>
                        <% } %>
                    </select>
                </div>
                <input type="hidden" name="categoryChoosed" id="category-choosed">

                <div class="date-time-section">
                    <div class="input-field">
                        <label for="task-date"><i class="fas fa-calendar-alt"></i> Due Date</label>
                        <input type="date" id="task-date" name="date" required>
                    </div>

                    <div class="input-field">
                        <label for="task-time"><i class="fas fa-clock"></i> Due Time (optional)</label>
                        <input type="time" id="task-time" name="time">
                    </div>
                </div>

                <input type="hidden" name="category" id="category-id">

                <div class="form-actions">
                    <button type="reset" class="btn btn-outline"><i class="fas fa-undo"></i> Reset</button>
                    <button type="submit" class="btn btn-primary"><i class="fas fa-save"></i> Create Task</button>
                </div>
            </form>
        </div>

        <!-- Category Form -->
        <div class="form-card">
            <h2><i class="fas fa-tags"></i> Create New Category</h2>
            <form id="category-form" action="/categories" method="POST">
                <div class="input-field">
                    <label for="category-name"><i class="fas fa-font"></i> Category Name</label>
                    <input type="text" id="category-name" name="name" placeholder="Enter category name" required>
                </div>

                <div class="input-field">
                    <label><i class="fas fa-palette"></i> Category Color</label>
                    <div class="color-picker-container">
                        <div class="color-option selected" style="background-color: #2196F3;" data-color="#2196F3"></div>
                        <div class="color-option" style="background-color: #4CAF50;" data-color="#4CAF50"></div>
                        <div class="color-option" style="background-color: #9C27B0;" data-color="#9C27B0"></div>
                        <div class="color-option" style="background-color: #FF9800;" data-color="#FF9800"></div>
                        <div class="color-option" style="background-color: #F44336;" data-color="#F44336"></div>
                        <div class="color-option" style="background-color: #607D8B;" data-color="#607D8B"></div>
                    </div>
                    <input type="hidden" id="category-color" name="color" value="#2196F3">
                </div>

                <div class="input-field">
                    <label><i class="fas fa-icons"></i> Category Icon</label>
                    <div class="icon-picker-container">
                        <div class="icon-option selected" data-icon="https://cdn-icons-png.flaticon.com/512/3281/3281289.png">
                            <img src="https://cdn-icons-png.flaticon.com/512/3281/3281289.png" alt="Work">
                        </div>
                        <div class="icon-option" data-icon="https://cdn-icons-png.flaticon.com/512/1077/1077012.png">
                            <img src="https://cdn-icons-png.flaticon.com/512/1077/1077012.png" alt="Personal">
                        </div>
                        <div class="icon-option" data-icon="https://cdn-icons-png.flaticon.com/512/4290/4290854.png">
                            <img src="https://cdn-icons-png.flaticon.com/512/4290/4290854.png" alt="Shopping">
                        </div>
                        <div class="icon-option" data-icon="https://cdn-icons-png.flaticon.com/512/7245/7245102.png">
                            <img src="https://cdn-icons-png.flaticon.com/512/7245/7245102.png" alt="Others">
                        </div>
                        <div class="icon-option" data-icon="https://cdn-icons-png.flaticon.com/512/3502/3502686.png">
                            <img src="https://cdn-icons-png.flaticon.com/512/3502/3502686.png" alt="Custom">
                        </div>
                    </div>
                    <input type="hidden" id="category-icon" name="icon" value="https://cdn-icons-png.flaticon.com/512/3281/3281289.png">
                </div>

                <div class="form-actions">
                    <button type="reset" class="btn btn-outline"><i class="fas fa-undo"></i> Reset</button>
                    <button type="submit" class="btn btn-secondary"><i class="fas fa-plus"></i> Create Category</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Task List Section -->
    <div class="task-list-section">
        <div class="section-header">
            <h2><i class="fas fa-list"></i> Recent Tasks</h2>
            <a href="/tasks/manage" class="btn btn-outline"><i class="fas fa-external-link-alt"></i> View All</a>
        </div>
        <div class="task-list-container">
            <table class="task-table">
                <thead>
                    <tr>
                        <th><i class="fas fa-file-alt"></i> Title</th>
                        <th><i class="fas fa-tag"></i> Category</th>
                        <th><i class="fas fa-calendar-alt"></i> Due Date</th>
                        <th><i class="fas fa-info-circle"></i> Status</th>
                        <th><i class="fas fa-cogs"></i> Actions</th>
                    </tr>
                </thead>
                <tbody id="task-list">
                    <% if (tasks && tasks.length > 0) { %>
                        <% tasks.slice(0, 5).forEach(function(task) { %>
                            <tr class="<%= task.completed ? 'completed-task' : '' %>">
                                <td><%= task.title %></td>
                                <td>
                                    <% const category = categories.find(c => c._id.toString() === task.category?.toString()); %>
                                    <% if (category) { %>
                                        <span class="category-badge" style="background-color: <%= category.color %>;">
                                            <%= category.name %>
                                        </span>
                                    <% } else { %>
                                        <span class="category-badge others">Others</span>
                                    <% } %>
                                </td>
                                <td><%= task.date %> <%= task.time ? task.time : '' %></td>
                                <td>
                                    <span class="status-badge <%= task.status %>">
                                        <%= task.status.charAt(0).toUpperCase() + task.status.slice(1) %>
                                    </span>
                                </td>
                                <td>
                                    <div class="actions">
                                        <form action="/tasks/<%= task._id %>/status" method="POST" style="display: inline;">
                                            <button type="submit" class="btn-icon toggle-status" data-status="<%= task.completed %>" title="<%= task.completed ? 'Mark as Pending' : 'Mark as Completed' %>">
                                                <i class="fas <%= task.completed ? 'fa-undo' : 'fa-check' %>"></i>
                                            </button>
                                        </form>
                                        <a href="/tasks/<%= task._id %>" class="btn-icon edit-task" title="Edit Task">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <form action="/tasks/<%= task._id %>" method="POST" style="display: inline;">
                                            <input type="hidden" name="_method" value="DELETE">
                                            <button type="submit" class="btn-icon delete-task" title="Delete Task" onclick="return confirm('Are you sure you want to delete this task?')">
                                                <i class="fas fa-trash"></i>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr class="no-tasks">
                            <td colspan="5">No tasks found. Create your first task above!</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>

    <!-- Category List Section -->
    <div class="category-list-section">
        <div class="section-header">
            <h2><i class="fas fa-tags"></i> Your Categories</h2>
            <a href="/categories" class="btn btn-outline"><i class="fas fa-external-link-alt"></i> View All</a>
        </div>
        <div class="category-grid">
            <% if (categories && categories.length > 0) { %>
                <% categories.forEach(function(category) { %>
                    <div class="category-card">
                        <img src="<%= category.icon %>" alt="<%= category.name %>" class="category-icon">
                        <div class="category-name" style="color: <%= category.color %>;">
                            <%= category.name %>
                        </div>
                        <div class="category-count">
                            <% const count = tasks.filter(t => t.category?.toString() === category._id.toString()).length; %>
                            <%= count %> task<%= count !== 1 ? 's' : '' %>
                        </div>
                        <% if (!category.isDefault) { %>
                            <div class="category-actions">
                                <a href="/categories/<%= category._id %>/edit" class="edit-btn">
                                    <i class="fas fa-edit"></i> Edit
                                </a>
                                <form action="/categories/<%= category._id %>/delete" method="POST" style="display: inline;">
                                    <button type="submit" class="delete-btn" onclick="return confirm('Are you sure you want to delete this category? Tasks will be moved to Others.')">
                                        <i class="fas fa-trash"></i> Delete
                                    </button>
                                </form>
                            </div>
                        <% } %>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="category-card" style="grid-column: 1 / -1; text-align: center;">
                    <p>No categories found. Create your first category above!</p>
                </div>
            <% } %>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Set default date to today
            const today = new Date().toISOString().split('T')[0];
            document.getElementById('task-date').value = today;

            // Color picker functionality
            const colorOptions = document.querySelectorAll('.color-option');
            const colorInput = document.getElementById('category-color');

            colorOptions.forEach(option => {
                option.addEventListener('click', function() {
                    // Remove selected class from all options
                    colorOptions.forEach(opt => opt.classList.remove('selected'));
                    // Add selected class to clicked option
                    this.classList.add('selected');
                    // Update hidden input value
                    colorInput.value = this.getAttribute('data-color');
                });
            });

            // Icon picker functionality
            const iconOptions = document.querySelectorAll('.icon-option');
            const iconInput = document.getElementById('category-icon');

            iconOptions.forEach(option => {
                option.addEventListener('click', function() {
                    // Remove selected class from all options
                    iconOptions.forEach(opt => opt.classList.remove('selected'));
                    // Add selected class to clicked option
                    this.classList.add('selected');
                    // Update hidden input value
                    iconInput.value = this.getAttribute('data-icon');
                });
            });

            // Category selection in task form
            const categorySelect = document.getElementById('task-category');
            const categoryIdInput = document.getElementById('category-id');
            const categoryChoosedInput = document.getElementById('category-choosed');

            categorySelect.addEventListener('change', function() {
                const selectedOption = this.options[this.selectedIndex];
                if (selectedOption) {
                    // Set the category ID for the hidden input
                    categoryIdInput.value = selectedOption.getAttribute('data-category-id') || '';
                    // Set the category name for categoryChoosed
                    categoryChoosedInput.value = selectedOption.getAttribute('data-category-name') || selectedOption.value;
                }
            });

            // Set up form submission
            document.getElementById('task-form').addEventListener('submit', function(e) {
                const selectedOption = categorySelect.options[categorySelect.selectedIndex];
                if (selectedOption && selectedOption.value) {
                    // Make sure both category ID and categoryChoosed are set
                    categoryIdInput.value = selectedOption.getAttribute('data-category-id') || '';
                    categoryChoosedInput.value = selectedOption.getAttribute('data-category-name') || selectedOption.value;
                } else {
                    e.preventDefault();
                    alert('Please select a category');
                }
            });

            // Form reset handlers
            document.getElementById('task-form').addEventListener('reset', function() {
                setTimeout(() => {
                    document.getElementById('task-date').value = today;
                    categoryIdInput.value = '';
                    categoryChoosedInput.value = '';
                    categorySelect.selectedIndex = 0;
                }, 0);
            });

            document.getElementById('category-form').addEventListener('reset', function() {
                setTimeout(() => {
                    // Reset color picker
                    colorOptions.forEach(opt => opt.classList.remove('selected'));
                    colorOptions[0].classList.add('selected');
                    colorInput.value = colorOptions[0].getAttribute('data-color');

                    // Reset icon picker
                    iconOptions.forEach(opt => opt.classList.remove('selected'));
                    iconOptions[0].classList.add('selected');
                    iconInput.value = iconOptions[0].getAttribute('data-icon');
                }, 0);
            });
        });
    </script>
</body>
</html>
